# csv-viewer — 项目约定（给 AI 协作时参考）

纯前端、单文件的 CSV 在线预览工具。面向人类的说明见 `README.md`；本文件记录**改代码时必须知道的红线与约定**。项目与同级目录 `../json-viewer`、`../xml-viewer` 保持同一套设计与代码风格。

## 红线（不要破坏）

- **单文件、零构建、零依赖**：全部 HTML / CSS / JS 都在 `index.html` 一个文件里。**禁止**引入打包器（Vite/Webpack 等）、框架（React/Vue 等）、npm 依赖或构建步骤。新增能力用原生 JS 实现。
- **纯前端、无后端**：所有解析与渲染在浏览器本地完成，不上传任何数据。「从 URL 导入」受浏览器 CORS 限制——目标接口必须允许跨域，否则无法读取。
- 主题用根元素 `html.dark` 类切换，配色全部走 `:root` / `html.dark` 里的 CSS 变量。新增颜色请复用已有变量，不要硬编码。

## 文件结构

- `index.html` — 全部代码（结构 + 样式 + 脚本）。
- `favicon.svg` — 蓝紫渐变圆角方块 + 白色「CSV」标志。
- `.github/workflows/deploy.yml` — GitHub Pages 部署：仓库根即站点根，push 到 `main` 自动部署。
- `README.md` — 面向用户的功能说明与在线访问地址。
- `CLAUDE.md` — 本文件，记录项目特有约定。

## CSV 解析与渲染约定

- **解析器手写**：基于 RFC 4180 实现，支持：
  - 引号包裹字段；
  - 引号内的逗号、换行、回车；
  - `""` 转义为单个双引号；
  - 自动识别 `\r\n`、 `\n`、 `\r` 作为行尾。
- **分隔符自动检测**：候选分隔符为逗号、制表符、分号；统计首行以外各候选出现频率（引号感知），取最高者为默认分隔符。用户可通过工具栏分段控件手动切换，切换后立即重新解析渲染。
- **表头开关**：默认开启，第一行作为列名；关闭时列名显示为「列1、列2…」。
- **排序**：点击表头三态循环（升序 → 降序 → 还原）。数字列（整列非空值均可解析为有限数字）按数值排序，否则按字符串 `localeCompare` 排序；还原时恢复原始行顺序。
- **搜索过滤**：所有列子串匹配，不区分大小写；过滤后实时更新「共 N 行 · 匹配 M 行」与表格渲染。
- **大文件截断渲染**：数据行超过 5000 行时，表格仅渲染前 2000 行，表底提示「仅显示前 2000 行，共 N 行」；复制、下载、导出 JSON 仍使用全量过滤后数据。
- **错误处理**：解析结果为空，或列数严重不一致（超过 50% 的数据行与首行列数不同）时，在 error-box 中提示并可编辑重试，样式与 json-viewer 保持一致。

## UI 约定

- **按钮文案保持极简**：每个按钮都带 SVG 图标，图标已承担表意，文字尽量短（如「导入」「粘贴」「下载」「示例」）。改按钮时沿用这个风格，不要写长句子。
- 工具栏按钮（导入 / 粘贴 / URL 导入 / 示例 / 复制全部 / 下载 / 导出 JSON）+ 分隔符分段控件（自动 / 逗号 / Tab / 分号）+ 表头开关。
- 页头右侧两个图标按钮：GitHub 源码链接（仓库 `https://github.com/wangruofeng/csv-viewer`，新标签页打开）、深浅色主题切换。

## 在线地址

已部署到 GitHub Pages：https://blog.wangruofeng007.com/csv-viewer/
